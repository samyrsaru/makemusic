# PM2 ecosystem configuration for MakeMusic API
# Place this in apps/api/ecosystem.config.cjs for reference

module.exports = {
  apps: [{
    name: 'makemusic-api',
    script: './dist/index.js',
    cwd: '/opt/makemusic',
    instances: 1,
    exec_mode: 'fork',
    env: {
      NODE_ENV: 'production'
    },
    env_dev: {
      NODE_ENV: 'development'
    },
    error_file: './logs/err.log',
    out_file: './logs/out.log',
    log_file: './logs/combined.log',
    time: true,
    autorestart: true,
    max_restarts: 10,
    min_uptime: '10s',
    watch: false,
    kill_timeout: 5000,
    listen_timeout: 10000,
    // Memory limits
    max_memory_restart: '500M',
    // Graceful shutdown
    shutdown_with_message: true,
    // Health check
    health_check_grace_period: 30000
  }]
}